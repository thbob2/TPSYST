#include <stdio.h>
#include <sys/types.h>
#include <unistd.h>
#include <sys/wait.h>
#include <stdlib.h>

int val,status;
pid_t pid,n1pid,n2pid,npid;

pid = (pid_t)getpid();
	npid = (pid_t)fork();
	if (npid == -1){ }
	else { if {}
		  else
		  }